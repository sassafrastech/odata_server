<%=
  if @countable
    o_data_json_feed(@query, @results, expand: @expand_navigation_property_paths).to_json
  else
    first_result = Array(@results).compact.first
    o_data_json_entry(@query, first_result, expand: @expand_navigation_property_paths, context: true).to_json
  end.html_safe
%>
